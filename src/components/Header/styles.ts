import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        marginTop: 36,
    },
    backButton: {
        position: 'relative',
        bottom: 25,
        right: 150,
    },
});